name: SSHX Ubuntu Session
on:
  workflow_dispatch:  # Manual trigger only

jobs:
  sshx-session:
    runs-on: ubuntu-latest
    steps:
    - name: Install SSHX
      run: |
        curl -fsSL https://sshx.io/get | sh

    - name: Start SSHX and get connection link
      run: |
        sshx
